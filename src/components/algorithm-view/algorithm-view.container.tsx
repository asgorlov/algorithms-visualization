import React, { FC } from "react";
import AlgorithmViewComponent from "./algorithm-view.component";

const AlgorithmViewContainer: FC = () => {
  return <AlgorithmViewComponent />;
};

export default AlgorithmViewContainer;
